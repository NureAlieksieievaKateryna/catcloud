server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/catcloud
    username: test
    password: 123
  jackson:
    time-zone: UTC
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
